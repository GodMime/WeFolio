<view class="text-section {{textSection.alignmentClass}}">
  <view wx:if="{{textSection.title}}" class="component-title">{{textSection.title}}</view>
  <text class="text-content" space="nbsp">{{textSection.content}}</text>
</view>

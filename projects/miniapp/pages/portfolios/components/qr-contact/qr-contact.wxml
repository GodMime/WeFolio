<view class="qr-section portfolio-theme-{{themeMode}}">
  <image
    wx:if="{{qrContact.qrUrl}}"
    class="qr-image"
    src="{{qrContact.qrUrl}}"
    mode="aspectFit"
    bindtap="handlePreviewQr"
  ></image>
</view>

const { request } = require('../../../utils/request.js')
const { createTeamContactLeadForm, submitTeamContactLead } = require('../utils/team-contact-leads.js')
const { normalizeTeamVisitorPortfolio, queryTeamVisitorSchedule, submitTeamVisitorEvent } = require('../utils/team-visitor-portfolio.js')
const { createIdempotencyKey, openTeamVisitorSession, requestWithTeamVisitorSessionRefresh, resolveTeamShareCode } = require('../utils/team-visitor-session.js')
const { uploadTeamVisitorProfile } = require('../utils/team-visitor-profile.js')
const { showTeamPortfolioUnavailableToast } = require('../utils/team-portfolios.js')

const TYPE_BUCKETS = Object.freeze({ TEAM_PROFILE: 'teamProfile', CAROUSEL: 'carousel', DIVIDER: 'divider', MEMBER_PORTFOLIO_GRID: 'grid', MEMBER_PORTFOLIO_LIST: 'list', TEXT_SECTION: 'text', SCHEDULE_QUERY: 'schedule', CONTACT_FORM: 'contact', QR_CONTACT: 'qr' })
const PERSONAL_VISITOR_URL = '/pages' + '/portfolios/visitor-portfolio/visitor-portfolio'
function buckets(items) { const value = { teamProfile: [], carousel: [], divider: [], grid: [], list: [], text: [], schedule: [], contact: [], qr: [] }; (Array.isArray(items) ? items : []).forEach((item) => { const key = TYPE_BUCKETS[item.componentType]; if (key) value[key].push(item) }); return value }
function isUncertainFailure(error) { return !error || !Number(error.statusCode) || Number(error.statusCode) >= 500 }

Page({
  data: { shareCode: '', sourceType: 'WECHAT_SHARE_CARD', loading: false, errorMessage: '', render: {}, componentBuckets: buckets([]), visitorKey: '', visitRecordId: 0, visitorProfileToken: '', visitorProfileAuthVisible: false, visitorProfileForm: { nickname: '', avatarPath: '' }, visitorProfileSaving: false, empty: true, underMaintenance: false, scheduleResults: {}, contactForms: {}, contactSubmitting: {}, contactModalVisible: {}, pendingOpenKey: '', pendingContactKeys: {} },
  onLoad(options = {}) { const shareCode = resolveTeamShareCode(options); if (!shareCode) { wx.showToast({ title: '暂无权限访问该团队作品集', icon: 'none' }); return }; this.setData({ shareCode, sourceType: options.scene && !options.shareCode ? 'QR_CODE' : 'WECHAT_SHARE_CARD' }); this.open() },
  async open() {
    if (this.data.loading || !this.data.shareCode) return
    this.setData({ loading: true, errorMessage: '' })
    const idempotencyKey = this.data.pendingOpenKey || createIdempotencyKey(); if (!this.data.pendingOpenKey) this.setData({ pendingOpenKey: idempotencyKey })
    try { const session = await openTeamVisitorSession({ shareCode: this.data.shareCode, sourceType: this.data.sourceType, idempotencyKey, requestFn: request }); this.applySession(session); this.setData({ pendingOpenKey: '' }) } catch (error) { if (this.handleUnavailableError(error)) return; this.setData({ errorMessage: '团队作品集暂不可访问' }) } finally { this.setData({ loading: false }) }
  },
  applySession(session) { const render = normalizeTeamVisitorPortfolio(session); this.setData({ render, componentBuckets: buckets(render.components), visitorKey: render.visitorKey, visitRecordId: render.visitRecordId, visitorProfileToken: render.visitorProfileToken, visitorProfileAuthVisible: render.needVisitorProfile, empty: render.components.length === 0, underMaintenance: render.underMaintenance }) },
  handleRetry() { this.open() },
  visitorRequest(options) { return requestWithTeamVisitorSessionRefresh({ shareCode: this.data.shareCode, sourceType: this.data.sourceType, requestFn: request, requestOptions: options, onRefresh: async (session) => this.applySession(session), refreshRequestOptions: (session, original) => Object.assign({}, original, { data: Object.assign({}, original.data, original.data && original.data.visitorProfileToken ? { visitorProfileToken: session.visitorProfileToken } : {}) }) }) },
  profileRequest(options) { const data = Object.assign({}, options.data, { visitorProfileToken: this.data.visitorProfileToken }); return this.visitorRequest(Object.assign({}, options, { data })) },
  handleUnavailableError(error) { return showTeamPortfolioUnavailableToast(error) },
  sendEvent(payload) { return submitTeamVisitorEvent((options) => this.visitorRequest(options), this.data.shareCode, Object.assign({}, payload, { idempotencyKey: createIdempotencyKey() })).catch((error) => { this.handleUnavailableError(error); return null }) },
  handleImagePreview(event) { const item = event.detail && event.detail.item; const componentKey = event.currentTarget.dataset.key; if (!item) return; this.sendEvent({ eventType: 'WORK_VIEWED', componentKey, workId: item.workId, mediaType: 'IMAGE' }); const url = item.mediaUrl || item.coverUrl; if (url) wx.previewImage({ current: url, urls: [url] }) },
  handleMemberPortfolio(event) { const detail = event.detail || {}; const componentKey = event.currentTarget.dataset.key; this.sendEvent({ eventType: 'MEMBER_PORTFOLIO_OPENED', componentKey, memberPortfolioId: detail.portfolioId }); if (detail.shareCode) wx.navigateTo({ url: `${PERSONAL_VISITOR_URL}?shareCode=${encodeURIComponent(detail.shareCode)}` }) },
  async handleScheduleQuery(event) { const detail = event.detail || {}; const child = this.selectComponent(`#schedule-${detail.componentKey}`); try { const result = await queryTeamVisitorSchedule((options) => this.visitorRequest(options), this.data.shareCode, { componentKey: detail.componentKey, queriedDate: detail.queriedDate, idempotencyKey: detail.idempotencyKey }); this.setData({ scheduleResults: Object.assign({}, this.data.scheduleResults, { [detail.componentKey]: result }) }); if (child && child.resolveQuery) child.resolveQuery({ detail: result }) } catch (error) { if (this.handleUnavailableError(error)) { if (child && child.rejectQuery) child.rejectQuery({ detail: { message: '当前团队作品集不可用', clearPendingIdempotencyKey: true } }); return }; if (child && child.rejectQuery) child.rejectQuery({ detail: { message: '档期查询失败，请重试', clearPendingIdempotencyKey: !isUncertainFailure(error) } }); wx.showToast({ title: '档期查询失败，请重试', icon: 'none' }) } },
  handleContactInput(event) { const detail = event.detail || {}; const componentKey = event.currentTarget.dataset.key; const pendingContactKeys = Object.assign({}, this.data.pendingContactKeys); delete pendingContactKeys[componentKey]; this.setData({ contactForms: Object.assign({}, this.data.contactForms, { [componentKey]: detail.form || {} }), pendingContactKeys }) },
  handleContactOpen(event) { const componentKey = event.currentTarget.dataset.key; this.setData({ contactModalVisible: Object.assign({}, this.data.contactModalVisible, { [componentKey]: true }) }); this.sendEvent({ eventType: 'CONTACT_FORM_EXPOSED', componentKey }) },
  handleContactClose(event) { const componentKey = event.currentTarget.dataset.key; this.setData({ contactModalVisible: Object.assign({}, this.data.contactModalVisible, { [componentKey]: false }) }) },
  async handleContactSubmit(event) { const componentKey = event.currentTarget.dataset.key; const form = event.detail && event.detail.form; if (this.data.contactSubmitting[componentKey]) return; const idempotencyKey = this.data.pendingContactKeys[componentKey] || createIdempotencyKey(); this.setData({ contactSubmitting: Object.assign({}, this.data.contactSubmitting, { [componentKey]: true }), pendingContactKeys: Object.assign({}, this.data.pendingContactKeys, { [componentKey]: idempotencyKey }) }); const child = this.selectComponent(`#contact-form-${componentKey}`); try { await submitTeamContactLead((options) => this.visitorRequest(options), this.data.shareCode, form, { visitRecordId: this.data.visitRecordId, sourceType: this.data.sourceType, idempotencyKey }); if (child && child.completeSubmit) child.completeSubmit({ detail: { success: true } }); const pendingContactKeys = Object.assign({}, this.data.pendingContactKeys); delete pendingContactKeys[componentKey]; this.setData({ contactForms: Object.assign({}, this.data.contactForms, { [componentKey]: createTeamContactLeadForm() }), contactModalVisible: Object.assign({}, this.data.contactModalVisible, { [componentKey]: false }), pendingContactKeys }); wx.showToast({ title: '已提交给团队', icon: 'success' }) } catch (error) { if (this.handleUnavailableError(error)) return; if (!isUncertainFailure(error)) { const pendingContactKeys = Object.assign({}, this.data.pendingContactKeys); delete pendingContactKeys[componentKey]; this.setData({ pendingContactKeys }) }; if (child && child.completeSubmit) child.completeSubmit({ detail: { success: false, message: '提交失败，请检查信息' } }); wx.showToast({ title: '提交失败，请检查信息', icon: 'none' }) } finally { this.setData({ contactSubmitting: Object.assign({}, this.data.contactSubmitting, { [componentKey]: false }) }) } },
  handleQrInteract(event) { const detail = event.detail || {}; this.sendEvent({ eventType: 'QR_CODE_INTERACTED', componentKey: detail.componentKey, action: detail.action }) },
  handleProfileInput(event) { const field = event.currentTarget.dataset.field; this.setData({ visitorProfileForm: Object.assign({}, this.data.visitorProfileForm, { [field]: event.detail.value }) }) },
  async handleChooseAvatar() { if (!wx.chooseMedia) return; try { const media = await new Promise((resolve, reject) => wx.chooseMedia({ count: 1, mediaType: ['image'], success: resolve, fail: reject })); this.setData({ visitorProfileForm: Object.assign({}, this.data.visitorProfileForm, { avatarPath: media.tempFiles[0].tempFilePath }) }) } catch (error) {} },
  handleProfileSkip() { this.setData({ visitorProfileAuthVisible: false }) },
  async handleProfileSave() { if (this.data.visitorProfileSaving) return; const form = this.data.visitorProfileForm; if (!form.nickname || !form.avatarPath) return wx.showToast({ title: '请完善头像和昵称', icon: 'none' }); this.setData({ visitorProfileSaving: true }); try { await uploadTeamVisitorProfile({ shareCode: this.data.shareCode, avatarPath: form.avatarPath, nickname: form.nickname, visitorProfileToken: this.data.visitorProfileToken, requestFn: (options) => this.profileRequest(options) }); this.setData({ visitorProfileAuthVisible: false }) } catch (error) { if (this.handleUnavailableError(error)) return; wx.showToast({ title: '资料保存失败，请重试', icon: 'none' }) } finally { this.setData({ visitorProfileSaving: false }) } },
  onShareAppMessage() { return { title: this.data.render.title, path: `/pages/team-portfolios/visitor-portfolio/team-visitor-portfolio?shareCode=${encodeURIComponent(this.data.shareCode)}` } }
})

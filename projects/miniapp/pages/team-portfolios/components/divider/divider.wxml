<view class="divider-component theme-{{themeMode}} {{editMode ? 'editor-mode' : ''}}">
  <block wx:if="{{editMode}}">
    <view class="editor-form">
      <view class="editor-field">
        <view class="editor-label">颜色</view>
        <view class="editor-choice-row">
          <text-color-editor color="{{colorValue}}" label="分割线颜色" active="{{editMode}}" bindchange="selectColor" />
        </view>
      </view>
      <view class="editor-field">
        <view class="editor-label">高度（像素）</view>
        <input class="editor-input" type="number" value="{{draft.heightPx}}" bindinput="changeHeight" />
      </view>
      <view wx:if="{{errorMessage}}" class="editor-error">{{errorMessage}}</view>
    </view>
  </block>
  <view wx:else class="line" style="height: {{config.heightPx || 16}}px; background: {{colorValue}};"></view>
</view>
